{
  "name": "HttpClient",
  "version": "0.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run": "babel src --out-dir lib && node lib/app.js" 
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dateformat": "^1.0.11",
    "sqlite3": "^3.1.0"
  }
}
